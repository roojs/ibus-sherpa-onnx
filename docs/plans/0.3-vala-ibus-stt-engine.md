# 0.3 Vala IBus speech dictation engine

**Status:** ⏳ proposed

Checklist: **`docs/guide-to-writing-plans.md`**. Vala: **`docs/coding-standards.md`** via **`docs/coding-standards-router.md`**.

**ℹ️** Parent / prior slices:
[`0.1-vala-sherpa-stdout-poc.md`](0.1-vala-sherpa-stdout-poc.md) (mic → sherpa → stdout),
[`0.2-vala-gtk-composer-stt.md`](0.2-vala-gtk-composer-stt.md) (GTK composer hello world — now a **sideline**).

**ℹ️** This plan is the **canonical** product path for the PoC. Cursor’s UI may also keep a copy under `.cursor/plans/`; edit **this file**.

## Agent rule: do not change the user’s system/session

**🚫 CRITICAL — read every time before running shell commands.**

Do **not** mutate the user’s desktop session or machine without an **explicit** ask in the current message. That includes (non-exhaustive):

- `gsettings` / `dconf` (input sources, shortcuts, anything)
- `ibus engine …`, `ibus restart`, switching IMEs
- starting/stopping/killing `stt-ibus-engine`, `ibus-daemon`, or other session processes
- `sudo`, `apt`, `meson install`, writing under `/usr`, `~/.config/ibus`, etc.
- adding input sources, grabbing Super+Space behaviour, “helpful” session tweaks

**Allowed without asking:** repo edits, `meson`/`ninja` **build** in this project tree, read-only inspection (`gsettings get`, `ibus engine` with no set, `pgrep`, reading files).

**When a session/install step is needed:** print the exact command(s), explain why, and **wait** for the user to run them (or to say “go ahead and run X”). Do not “just try it” on the live session.

**Why:** Phase 1 runtime registration is easy to “fix” by poking GNOME/IBus; that left the session in a confusing state. User must stay in control.

## Purpose

- **🔷** Convert the PoC into a real **IBus input-method engine** (daemon process), so dictation lands in **whatever text field has focus** — not a hacked GTK composer, not AT-SPI / ydotool injection.
- **🔷** Reuse [`src/stt_engine.vala`](../../src/stt_engine.vala): GStreamer mic → sherpa-onnx Nemotron; `partial` / `endpoint` already Idle-marshalled to the main loop.
- **🔷** Map `partial` → `IBus.Engine.update_preedit_text`; `endpoint` → `commit_text` (then keep listening until stop).
- **🔷** Activation: engine idle (mic off) until an explicit **toggle hotkey**; do **not** open the mic merely because the Speech IME is selected.
- **🔷** **One hotkey toggles listen on/off** (press again to stop). No Escape-to-stop, no “typing stops dictation.”
- **🔷** Hotkey **remappable** via a small config file (not environment variables — hostile UX). Default `Ctrl+Shift+Space`.
- **🔷** Keep CLI `stt-poc` for recognizer smoke tests.
- **💩** Park GTK composer under `src/gtk/` as a sideline demo (`stt-gtk-poc`); not the main integration story.

## Installs (do not hack around)

**🚫** Do not invent alternate stacks, skip pkg-config, vendor headers, or “work without the -dev package.” If a package or install step is required, **stop and tell the user the exact command**; wait for them to run it.

### Already present on this machine

- `ibus`, `libibus-1.0-5`, GTK IBus modules (session IBus stack)
- **`libibus-1.0-dev`** `1.5.32~rc2-1` — `pkg-config ibus-1.0` + Vala VAPI ✔️
- sherpa-onnx `.deb`s, GStreamer, Vala/meson (from 0.1 / 0.2)

### Required before Phase 1 compile

```bash
sudo apt-get install -y libibus-1.0-dev
```

Provides `pkg-config --exists ibus-1.0`, headers, and the Vala `ibus-1.0` VAPI. **✔️ Already installed** on this machine.

### Required to use the engine in the session (Phase 3)

- Build/install a **Debian package** that drops the component XML + libexec binary (see Phase 3). That **is** registration.
- User may run `ibus restart` (or re-login) once so the session rescans components — documented in README; **not** a clever postinst.
- User selects Speech STT in GNOME input sources like any other IME; points `~/.config/stt-ibus/model` at weights (symlink or dir).

**🚫** Do not treat hand-launched `register_component` as the product install path. **🚫** Do not put model weights in the `.deb`.

## How IBus activation works

IBus is a **system input-method bus**, not “run a daemon and inject keys.”

```
  [Focused app] → toolkit IBus client → ibus-daemon → stt-ibus-engine
       ↑                                                      │
       └──── preedit / committed text ←───────────────────────┘
```

1. **`ibus-daemon` must be running** (desktop session usually starts it).
2. User **selects** our engine in the IBus panel (e.g. “Speech STT”) — same as switching to Pinyin. That makes us the **active IME**.
3. When a text field has focus, key events go to our engine (`process_key_event`).
4. We show **preedit** (`update_preedit_text`) and **commit** (`commit_text`) into that focused widget.
5. ibus-daemon **spawns** our process from the component XML `<exec>` when needed.

### Why not “always listen while IME selected”

Selecting the Speech IME only means we receive keys when a text field is focused. It does **not** mean “open the mic.” Auto-listening on enable/focus would be surprising, privacy-hostile, and wasteful. Mic stays off until the start hotkey.

### Toggle hotkey (start and stop)

- **Behaviour:** the same accelerator **toggles** listening. First press → `start()`; second press → `stop()`, hide/cancel preedit (commit any pending partial only if we already have an endpoint policy — PoC: discard unfinished preedit on stop).
- **Default:** `Ctrl+Shift+Space` (matches 0.2).
- **Config (no env vars):** read `~/.config/stt-ibus/hotkey` (one accelerator line, e.g. `Ctrl+Shift+Space`). Missing/empty → default. Document in README how to edit that file.
- **🚫** Do not use `STT_*` environment variables for the hotkey (or other user-facing settings in this slice).
- **While listening:** other keys **pass through** (`return false`) so normal typing still works; only the toggle key is consumed.
- **Later (not this slice):** ibus-setup / GSettings prefs UI instead of hand-editing the file.
- **Constraint:** hotkey only arrives when **this IME is active** and a client is focused. Global shortcuts with another IME selected are out of scope.

## Suggested order

1. **🔷** `✔️` Phase 0 — this plan + move GTK under `src/gtk/`
2. **🔷** `✔️` Phase 1 — `libibus-1.0-dev` + minimal IBus engine skeleton (build/register)
3. **🔷** `✔️` Phase 2 code — `SttEngine` + toggle → preedit/commit (**⏳** user ✅ live dictation)
4. **🔷** `⏳` Phase 3 — **Debian package** installs component + binary; remove package cleans up

---

## Phase 0 — Docs + tree shuffle

**Why:** Canonical plan lives here; GTK is clearly a sideline.

- **🔷** `✔️` This file (`docs/plans/0.3-vala-ibus-stt-engine.md`).
- **🔷** `✔️` Move GTK sources to `src/gtk/`; update `meson.build` for `stt-gtk-poc`.
- **💩** `✔️` README: IBus is the main path; GTK is a sideline.

### Phase 0 success

- **🔷** `✔️` Plan in `docs/plans/`; GTK paths under `src/gtk/`; meson still builds `stt-poc` + `stt-gtk-poc`.

---

## Phase 1 — Minimal IBus engine (no STT yet)

**Why:** Prove bus registration before wiring audio.

- **🔷** `✔️` **User install:** `libibus-1.0-dev` present (`pkg-config ibus-1.0` works).
- **🔷** `✔️` Meson target `stt-ibus-engine`, `dependency('ibus-1.0')`.
- **🔷** `✔️` `src/ibus/ibus_main.vala` + `IBus.Engine` subclass (Phase 2 adds STT).
- **ℹ️** GNOME Settings / Super+Space often **won’t** list a runtime-registered engine; CLI `ibus engine stt-speech` can select it while the process runs. Phase 3 install is the real GNOME path. Do **not** poke the user’s session to “prove” Phase 1.

### Phase 1 success

- **🔷** `✔️` Binary builds and registers with `ibus-daemon` when the user runs it.

---

## Phase 2 — Wire SttEngine + remappable toggle key

**Why:** User-facing win: speak → text in the focused app.

- **🔷** `✔️` Own one `SttEngine` via `SttIbusApplication` (`GLib.Application`); model at `~/.config/stt-ibus/model` only (directory or symlink — no CLI override).
- **🔷** `✔️` Load toggle accelerator from `~/.config/stt-ibus/hotkey`, else default `Ctrl+Shift+Space` (parsed via `IBus.accelerator_parse` / `key_event_from_string`).
- **🔷** `✔️` Toggle key → if not listening `start()`, else `stop()` + hide preedit. No Escape / typing stop paths.
- **🔷** `✔️` Other keys always pass through while listening.
- **🔷** `✔️` `partial` → `update_preedit_text`; `endpoint` → `commit_text` + hide preedit; stay listening until toggle off.
- **🔷** `✔️` No GTK/Adwaita dep on the IBus binary (IBus accelerator APIs only).

### Phase 2 success

- **🔷** `⏳` User ✅: select Speech IME → focus a text field → toggle on → speak → text appears → toggle off; remapped hotkey via config file works.
- **ℹ️** Agent must not run session switch commands; give the user the smoke steps and wait.

---

## Phase 3 — Debian package = register / unregister

**Why:** Runtime `register_component` from a hand-launched binary does **not** show up in GNOME’s input sources. Real IBus engines (e.g. `ibus-pinyin`) register by **shipping files**; `ibus-daemon` discovers them and **spawns** the engine when needed. A `.deb` install/remove is that lifecycle — not clever maintainer scripts.

### How registration works (no clever postinst)

Same pattern as `ibus-pinyin`:

| Install path | Role |
| --- | --- |
| `/usr/share/ibus/component/stt-speech.xml` | Declares the engine; `<exec>/usr/libexec/ibus-engine-stt-speech --ibus</exec>` |
| `/usr/libexec/ibus-engine-stt-speech` | Engine binary (our `stt-ibus-engine`, renamed on install) |

- **Install package** → those files appear → after a normal session IBus reload/restart (user may run `ibus restart` once, or log out/in), **Speech STT** shows in GNOME input sources. Daemon starts the binary with `--ibus` when the IME is used.
- **Remove package** → dpkg deletes those files → engine disappears. No postrm “unregister” API needed.
- **🚫** No postinst that runs the engine, calls `ibus engine …`, edits gsettings, or downloads models.
- **🚫** No relying on runtime `register_component` for the packaged product path (keep it only as an optional unpackaged dev aid, or drop it when `--ibus` is implemented).

### Engine binary changes for packaging

- **🔷** `⏳` Support **`--ibus`**: when ibus-daemon spawns us, set up `Factory` + `request_name` and **do not** call `register_component` (daemon already knows us from the XML). Match upstream engine mains / pinyin’s `--ibus` exec line.
- **🔷** `⏳` Model still **not** in the `.deb` (~hundreds of MB). User keeps `~/.config/stt-ibus/model` as a directory or symlink (document in README). Package `Depends:` on `ibus`, `libsherpa-onnx-c-api1`, GStreamer runtime bits, etc.

### Packaging deliverables

- **🔷** `⏳` `data/stt-speech.xml` (component; exec → `/usr/libexec/ibus-engine-stt-speech --ibus`).
- **🔷** `⏳` Meson `install: true` for the engine binary to `libexec` with the packaged name (or `debian/*.install` rename).
- **🔷** `⏳` `debian/` package (e.g. `ibus-stt-speech` or `stt-ibus`): `control`, `rules`, `changelog`, `copyright`, `.install` for binary + XML. Build with normal `dpkg-buildpackage` / whatever this repo already uses for sherpa.
- **🔷** `⏳` README: build `.deb`, `sudo apt install ./…deb`, symlink model, `ibus restart` (user runs), add Speech STT in Settings → Keyboard, toggle hotkey / `~/.config/stt-ibus/hotkey`. Uninstall: `sudo apt remove …`.
- **💩** Keep `stt-poc` / `stt-gtk-poc` out of that binary package (or separate packages later).

### Phase 3 success

- **🔷** `⏳` User installs the `.deb` → engine listed in GNOME / `ibus list-engine` after IBus reload → select Speech STT → toggle → dictate. Remove `.deb` → engine gone from the list.

---

## Repo layout (target)

```
gtk-speechtotext-poc/
  docs/plans/0.3-vala-ibus-stt-engine.md
  debian/                                  # ibus-stt-speech (or similar)
  data/stt-speech.xml                      # → /usr/share/ibus/component/
  src/stt_engine.vala
  src/sherpa-onnx.vapi
  src/main.vala
  src/ibus/
    ibus_main.vala
    ibus_application.vala                  # --ibus + --debug
    stt_ibus_engine.vala
  src/gtk/                                 # sideline
  meson.build
```

---

## Explicit non-goals

- **🚫** AT-SPI / ydotool / clipboard paste into arbitrary apps
- **🚫** Fcitx5 in this slice
- **🚫** OLLMchat tree changes
- **🚫** Wake-word / always-on mic
- **🚫** Cloud STT
- **🚫** Skipping Debian packaging in favour of “just run the binary” as the supported install
- **🚫** Clever `postinst`/`prerm` that starts the engine, switches IME, or edits gsettings
- **🚫** Shipping Nemotron weights inside the `.deb`
- **🚫** Environment variables for hotkey (or other user-facing settings)
- **🚫** Escape / typing-as-stop (toggle key only)
- **🚫** Changing the user’s GNOME/IBus session (gsettings, `ibus engine`, killing/starting session processes) without an explicit ask — see **Agent rule** at top

## LLM notes

- Implement only after this plan is approved.
- Re-read **Agent rule: do not change the user’s system/session** before any shell that is not clearly build/read-only.
- **Stop and ask the user** for any missing apt package, privileged install, IBus restart, or engine select; do not work around it and do not run those commands yourself unless told to.
- When Phase 3 lands, `.deb` install / model symlink / `ibus restart` / select IME go in the **README**.
- Read coding-standards via router before Vala.
- Do not invent IBus APIs — follow `ibus-1.0` VAPI / packaged engines (`--ibus` + component XML).
- Do not emit IBus UI updates from the GStreamer thread — `SttEngine` already Idle-marshals.
- Do not mark backlog **✅**; user verifies after **package install**.
- Do not tell the user to `ibus engine stt-speech` as the supported workflow once packaging exists.

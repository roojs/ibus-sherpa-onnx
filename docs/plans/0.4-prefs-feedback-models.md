# 0.4 Preferences, listening feedback, and system models

**Status:** ⏳ proposed

Checklist: **`docs/guide-to-writing-plans.md`**. Vala: **`docs/coding-standards.md`** via **`docs/coding-standards-router.md`**.

**ℹ️** Parent: [`0.3-vala-ibus-sherpa-onnx.md`](0.3-vala-ibus-sherpa-onnx.md) (IBus engine + `.deb`). This slice is UX + prefs + model install story.

**ℹ️** Carry forward **Agent rule** from 0.3: do **not** mutate the user’s GNOME/IBus session, run `sudo`/`pkexec` against their machine, or `dpkg -i` without an explicit ask. Repo edits + local `ninja` / `dpkg-buildpackage` OK.

## Purpose

- **🔷** Real **Preferences** for the IME (GNOME’s “Preferences” / component `<setup>`), not a dead menu entry.
- **🔷** First-class **hotkey** editor (press-a-key capture) in prefs — same idea as RooTerm `RowKeySelect` (copy the small pattern; do not depend on RooTerm).
- **🔷** User prefs in one **`GLib.KeyFile`**: `~/.config/ibus-sherpa-onnx/settings.ini` (not GSettings; not one file per flag). `model` stays a symlink beside it.
- **🔷** Clear **listening feedback** so the user is never guessing: animated preedit `…`, top-bar / panel **symbol**, optional **desktop notification**.
- **🔷** Fix **terminal shortcuts** broken while this IME is active (`Ctrl+C`, `Ctrl+Shift+R`, etc.) — caused by blanket `forward_key_event`.
- **🔷** While **listening**, any normal key (not the toggle) **stops dictation** (mic off / clear preedit), then the key is delivered to the client — typing interrupts recording.
- **🔷** Fix **VTE / plain keys** still eaten or broken after Phase A’s modifier pass-through: **Enter**, **Delete/Backspace**, arrows, and similar non-printable keys must reach the terminal (and Text Editor); do not leave the client unable to submit or edit after dictation or while Sherpa is selected.
- **🔷** Prefs **Select model** (**560** / **1120** ms only; default **None**). On close: download+extract as user → **polkit moves** into system `models/`; then set **user** `model` symlink to that tree (no polkit for selection).
- **🔷** System holds **multiple** installed trees; **active choice is always the user symlink** `~/.config/ibus-sherpa-onnx/model` → one system dir. No system-level “active model” symlink.
- **🚫** No separate “user override model tree” / parallel user `models/` store in the prefs story.
- **🚫** No `MODELS_DIR` / model-path environment knobs. **🚫** No hand-crafted `ln -s` for normal install.
- **🚫** Privileged helper must **not** download from the network (polkit = move into `models/` + cache cleanup only).
- **🚫** No mscorefonts-style silent `postinst` network download. Engine `.deb` stays small.

## Problems to close (from 0.3 soak)

| Issue | Notes |
| --- | --- |
| Hard to tell if recording | Prefs-driven feedback: preedit `…` animation (default on); IBus **symbol** on the indicator; optional **desktop notifications** on toggle |
| Top bar shows cryptic `en` / `en2` | Engine language stays `en` for locale grouping, but **`<symbol>` must be explicit** (default **`voi`**) so the badge is not a bare language code. While listening, switch symbol (e.g. `voi` → `…` or `rec`) |
| Panel “Mic off” row feels useless | Keep property for completeness; **symbol + preedit (+ notify)** are the primary cues |
| GNOME Preferences does nothing | Ship `ibus-setup-sherpa-onnx` + `<setup>` in `data/sherpa-onnx.xml` + `.desktop` |
| Hotkey only via hand-edited file | Prefs UI writes KeyFile; engine reloads on focus/toggle or document restart |
| VTE / apps: Ctrl+C etc. dead | Do **not** `forward_key_event` for Ctrl/Alt/Super combos — `return false` and let the client handle; keep forward (or capability-aware path) only for plain typing if still needed |
| VTE: Enter / Delete / arrows dead | After soak: printable forward path still blocks or mishandles **Return**, **BackSpace**, **Delete**, navigation — client never sees them (or only after weird “repeat”). Retest in VTE + Text Editor; prefer `return false` for non-printables unless a proven forward path is required |
| Typing while listening keeps recording | While mic is on, a non-toggle keypress should **stop listening** (discard or commit policy TBD — default: stop + clear preedit), then pass the key through so the user can type immediately |
| `has_focus=false` dropped partials | Already fixed in engine (do not reintroduce a hard `has_focus` gate) |
| Models only via manual symlink | System `models/` store + user `model` → symlink; prefs select; auto-link largest if missing |
| Prefs offered useless small chunks | Offer **560** / **1120** only (80 / 160 ≈ same size, little benefit) |

## Model locations

**Less config:** system stores weights; user config is only which one (a symlink). No path env vars.

### System (shared store — not “the active model”)

```
/usr/share/ibus-sherpa-onnx/models/<nemotron-dir-name>/
```

Many directories may coexist. There is **no** `/usr/share/ibus-sherpa-onnx/model` active symlink.

### User (active selection)

```
~/.config/ibus-sherpa-onnx/model  →  /usr/share/ibus-sherpa-onnx/models/<chosen>/
```

Prefs / engine create or retarget this symlink as the user (no polkit). That is the only “which model” config.

### User staging (download only)

```
~/.cache/ibus-sherpa-onnx/download/<chunk-or-dir>/   # archive + unpack (as user)
```

After polkit **move**, delete that cache tree (and leftover `.tar.bz2`).

### Engine / first-run resolution

Load **`~/.config/ibus-sherpa-onnx/model`** (must resolve to a model directory).

If that path is **missing**:

1. If `/usr/share/ibus-sherpa-onnx/models/` has one or more complete installs → **auto `ln -sfn`** to the **largest** installed tree (by on-disk size), then use it. New users on a machine that already has models get a working default without opening prefs.
2. If no system models yet → fail clearly / prefs starts at **None** until the user picks one (download + install).

If `model` already exists (symlink or, **💩** legacy real directory), use it as-is until prefs changes the selection.

**ℹ️** Package creates `/usr/share/ibus-sherpa-onnx/models/` (empty). It does **not** ship weights.

## Download + polkit install flow

On prefs close with a selected model that is not already active:

1. **User download** (no privileges): prefs uses an in-process Soup downloader into `~/.cache/ibus-sherpa-onnx/download/…`. Show a **`Gtk.ProgressBar`** (bytes / total, optional rate text).
2. **User extract** (no privileges, in-process): unpack the `.tar.bz2` with **libarchive** in Vala (not `tar`/`GLib.Process` shell-out). Same progress bar, label switches to “Extracting…”. Fraction from compressed bytes consumed / archive size (or uncompressed written / sum of entry sizes if easy). Yield to the main loop so the bar updates (async or idle chunks).
3. **Polkit move** (privileged, narrow): `pkexec` a small helper that:
   - **moves** (not copies) the unpacked dir into `/usr/share/ibus-sherpa-onnx/models/<dir>/` (skip move if that dir already exists and looks complete);
   - **deletes** the user’s cache staging for that download;
   - does **not** touch any “active model” symlink (that is user-owned).
4. **User symlink** (no privileges): `ln -sfn` `~/.config/ibus-sherpa-onnx/model` → the chosen system tree.
5. If the variant is **already** under `models/` and the user only changed selection: only step 4 — **no** download, **no** polkit.
6. If download/extract/move fails: leave prior user symlink intact when possible; clear partial cache on cancel when practical.

### Reuse OLLMchat download class (pattern)

**🔷** Lift / adapt the Hugging Face download path from **OLLMchat** — `OLLMhf.Download` (`gitlive/OLLMchat/libochf/Download.vala`: Soup session, resume via `.partial` + Range, `progress` signal). UI wiring idea: OLLMchat `ActivityBanner` handling `event.hf.download.progress` → fraction on a progress bar.

- **Copy the small class/pattern into this repo** (same rule as RooTerm hotkey capture): do **not** add a runtime dependency on OLLMchat / `libochf` / `OLLMrpc`.
- Adapt for **one GitHub release `.tar.bz2`** (Nemotron), not Hub GGUF siblings / `download.json` manifest — keep: Soup GET, Content-Length progress, cancellable, optional resume.
- Prefs binds download `progress` → **`Gtk.ProgressBar`** (“Downloading… 120 MB / 440 MB”).

### Extraction progress (Vala / libarchive)

Shelling out to `tar` gives little usable progress without awkward checkpoint parsing. Prefer **in-process libarchive** (`libarchive` pkg + system Vala vapi):

- **🔷** `Archive.Read` open the downloaded `.tar.bz2`; `Archive.WriteDisk` (or equivalent) write entries under the cache staging dir.
- **🔷** Emit extract progress on the same bar: e.g. `position_compressed()` / archive file size after each entry or data block; update label “Extracting…”.
- **🔷** Keep UI responsive (`Idle.add` / async chunks between entries or every N MiB).
- **🚫** Do not use silent `tar xf` with only a pulsing indeterminate bar as the primary UX (pulse OK only as brief fallback).
- **💩** Exact uncompressed totals are nicer but optional; compressed-stream fraction is enough for 0.4.

**💩** SHA-256 / Hub ETag verify can stay out of 0.4 unless cheap.

Helpers:

- Prefs Vala downloader (primary UX).
- [`scripts/fetch-nemotron-model.sh`](../../scripts/fetch-nemotron-model.sh) — keep for CLI/dev cache fetch (wget OK there).
- `/usr/libexec/ibus-sherpa-onnx-install-model` — polkit **move into `models/` + cache rm** only (path allowlist). No user-config writes as root.
- Polkit policy (“Install Sherpa ONNX speech model”).

**🚫** Do not download as root / inside the polkit helper.  
**🚫** Do not maintain a system-wide active-model symlink.  
**🚫** Do not embed sudo password prompts in Vala — **pkexec** only.  
**🚫** Do not download from `postinst`.  
**🚫** Do not link against OLLMchat libs.

CLI note: today’s euid→config/share script is **interim**; align or document when Phase D lands.

After success: restart engine / `ibus restart` if already running (or document reload).

## Preferences UI (setup binary)

- **🔷** Binary: `ibus-setup-sherpa-onnx` → `/usr/libexec/ibus-setup-sherpa-onnx` (GTK4 + libadwaita; only this binary needs Adwaita).
- **🔷** Wire `data/sherpa-onnx.xml` `<setup>/usr/libexec/ibus-setup-sherpa-onnx</setup>`.
- **🔷** `.desktop` (NoDisplay OK) so IBus/GNOME can launch it — same pattern as `ibus-setup-pinyin`.
- **🔷** Window contents (one page is enough):
  - **Toggle hotkey** — press-to-capture (RooTerm `RowKeySelect`-style).
  - **Desktop notifications** — switch (default **off**).
  - **Preedit listening animation** — switch (default **on**).
  - **Select model** — radio/list of Nemotron chunk variants; each row shows a short label + **download size** (approx OK). Mark which variants are already present under system `models/`. Starts with **None** when nothing usable is selected/installed. Pre-select from the user `model` symlink target when detectable.
  - **Progress bar** — shown for download **and** extract (label switches); hide when idle. **🚫** Pulse-only with no numbers. Download must show **MB transferred / ~total** and **percent** on the bar (interim: poll growing `.tar.bz2` on disk; Phase D: Soup Content-Length). Extract label at least (“Extracting…”); prefer real extract fraction in Phase D.
- **🔷** Persist prefs to **`~/.config/ibus-sherpa-onnx/settings.ini`**:

```ini
[general]
hotkey=Ctrl+Shift+Space
notifications=false
preedit-animation=true
```

- **🔷** **Apply on close:** save KeyFile; then model pipeline if needed:
  - selection already installed → retarget user `model` symlink only;
  - selection missing under `models/` → Soup download → libarchive extract → pkexec move → user `ln -sfn` → clear cache.
  - May keep the window open until finished. No separate Download button.
- **🚫** Do not start download on radio click alone — only after close.
- **🚫** Do not add more one-key scrap files under config. **🚫** GSettings (KeyFile chosen instead).

## Panel badge (not “en”)

GNOME’s input-source indicator falls back to the **language** code when `<symbol>` is empty — hence useless **`en` / `en2`**.

- **🔷** Set component XML `<symbol>voi</symbol>` (short for voice; alternatives considered: `mic`, `dict`, `stt` — pick one and keep it).
- **🔷** Keep `<language>en</language>` so Settings still groups under English.
- **🔷** Runtime: idle badge **`voi`**; listening **`…`** or **`rec`** (via property / engine symbol update — whatever GNOME actually mirrors to the top bar; verify on Shell 48).
- **🚫** Do not leave symbol empty. **🚫** Do not use bare `en` as the badge.

## Listening feedback (engine)

- **🔷** While listening and no partial yet: timer updates preedit through `.` → `..` → `…` (or similar); stop animation when partials arrive or listening stops.
- **🔷** Sync panel **symbol** with listening state (`voi` ↔ `…` / `rec`); property label can stay “Mic off” / “Listening…”.
- **🔷** If notifications enabled: `GLib.Notification` / `GNotification` via a session bus — short “Listening…” / “Mic off” on toggle (no spam on every partial).
- **💩** Panel property row can stay; not the primary cue.

## Key pass-through fix

- **🔷** Toggle combo: handle and `return true`.
- **🔷** Keys with **Ctrl / Alt / Super / Meta / Hyper** (and not the toggle): **`return false`** — do not forward (restores terminal and app accelerators).
- **🔷** Plain typing (no those mods, or Shift-only for capitals): keep whatever works on GNOME (capability-aware `forward_key_event` + `true`, or `false` if sufficient after retest).
- **🔷** **Non-printables** (Return, BackSpace, Delete, Tab, arrows, Home/End, etc.): must not stay stuck in a forward-only path that VTE ignores — retest; likely **`return false`** so the client indexes/handles them (same failure mode as “can’t press Enter or Delete after dictation”).
- **🔷** While **listening**: on any non-toggle key (printable or edit key), **stop listening** first (clear listening UI / preedit), then apply the pass-through rules above so the interrupting key reaches the app.
- **🔷** Retest: GNOME Text Editor typing + VTE `Ctrl+C` / `Ctrl+Shift+R` / **Enter** / **Backspace** / **Delete** with Sherpa selected; interrupt listening by typing a letter.

## Suggested order

1. **🔷** Phase A — Key pass-through fix (unblocks daily use in terminals).
2. **🔷** Phase B — Listening feedback (animation + symbol + notification hook gated by config).
3. **🔷** Phase C — Setup binary + hotkey / notification / animation prefs + **Select model** UI + XML `<setup>`.
4. **🔷** Phase D — Close → download/extract → polkit move/cache-rm → user `model` symlink; auto-link largest if missing.
5. **🔷** Phase E — README + `.deb` (helpers, polkit, empty `models/`); user verifies.

---

## Phase A — Terminal / shortcut pass-through

- **🔷** `✔️` Adjust [`src/engine.vala`](../../src/engine.vala) `process_key_event` for Ctrl/Alt/Super → `return false` (initial pass).
- **🔷** `⏳` Fix remaining VTE breakage: Enter / Delete / Backspace / arrows (and similar) with Sherpa selected — stop eating or mis-forwarding them.
- **🔷** `⏳` While listening: non-toggle key stops dictation, then delivers the key to the client.
- **🔷** `⏳` Smoke: Text Editor still types; VTE Ctrl+C, Enter, Backspace work; typing while listening stops the mic.

### Phase A success

- **🔷** `⏳` User ✅: shortcuts + Enter/Delete work in VTE; toggle still works; typing interrupts listening; dictation still works in Text Editor.

---

## Phase B — Listening feedback

- **🔷** `✔️` Component `<symbol>voi</symbol>` (and runtime EngineDesc if unpackaged path still registers).
- **🔷** `✔️` Preedit animation while listening (before first partial).
- **🔷** `✔️` Badge symbol idle `voi` / listening `…`; property label Mic off / Listening….
- **🔷** `✔️` Optional `GNotification` when `notifications` enabled.
- **🔷** `✔️` Read notification / animation / hotkey from `settings.ini` KeyFile (defaults: notify off, animation on).

### Phase B success

- **🔷** `⏳` User ✅: top bar never shows bare `en` for this engine; listening state obvious; VTE still weak on preedit but notify/badge help.

---

## Phase C — Preferences (setup)

- **🔷** `⏳` `src/setup/` Adwaita app: hotkey capture, toggles, **Select model** (None + **560** / **1120** only; show “installed” when present under system `models/`).
- **🔷** `⏳` On close: save prefs; if selection needs work, kick Phase D pipeline (may keep window alive with progress until done).
- **🔷** `⏳` Meson install libexec + desktop; component XML `<setup>`.
- **🔷** `⏳` Prefs writes `settings.ini`; engine reloads KeyFile on activate or focus_in if cheap.

### Phase C success
…
- **🔷** `⏳` User ✅: GNOME IME **Preferences** opens; hotkey works; Select model shows sizes; close with a selection starts the install flow.

---

## Phase D — Cache download + polkit move + user symlink

- **🔷** `⏳` Engine: resolve `~/.config/…/model`; if missing and system `models/` non-empty → auto-link **largest** install; drop system active-symlink fallback.
- **🔷** `⏳` Copy/adapt OLLMchat `OLLMhf.Download` → in-repo Soup downloader + `progress` → prefs `Gtk.ProgressBar`; no OLLMchat dep.
- **🔷** `⏳` In-process **libarchive** extract with progress on the same bar (build-dep `libarchive-dev` for setup binary only).
- **🔷** `⏳` Polkit policy + `/usr/libexec/ibus-sherpa-onnx-install-model`: **mv** into `models/` + **rm** cache staging only (path allowlist).
- **🔷** `⏳` Prefs/engine set user `model` → chosen system dir; switch selection = symlink only when already installed.
- **🔷** `⏳` Failure path: errors shown; previous user symlink left intact when possible.
- **🔷** `⏳` Align CLI fetch script with cache→polkit→user-symlink (or mark interim euid behaviour deprecated in README).

### Phase D success

- **🔷** `⏳` User ✅: pick model → close → download → polkit move → user symlink → cache gone → dictate; second variant coexists; switch is symlink-only; new account auto-gets largest installed model.

---

## Phase E — Docs / package

- **🔷** `⏳` README: prefs Select model; cache→polkit; system multi-model paths; feedback options.
- **🔷** `⏳` `debian/` installs setup binary, fetch/install helpers, polkit policy, `/usr/share/ibus-sherpa-onnx/models/`.
- **💩** RPM later (same files).

### Phase E success

- **🔷** `⏳` User ✅: reinstall `.deb`, end-to-end prefs + download + polkit move + dictate.

---

## Repo layout (additions)

```
src/Config.vala                         # shared KeyFile settings (engine + setup)
src/setup/main.vala
src/setup/Row.vala                      # RooTerm Dialog.Row pattern
src/setup/RowKeySelect.vala
src/setup/RowSwitch.vala
src/setup/Preferences.vala              # Adw.Window + PreferencesPage (RooTerm shell)
data/sherpa-onnx.xml                    # + <setup>
data/ibus-setup-sherpa-onnx.desktop
data/polkit/org.roojs.ibus-sherpa-onnx.policy
scripts/fetch-nemotron-model.sh
scripts/install-model.sh                # Phase D: pkexec move into models/ + rm cache
# /usr/share/ibus-sherpa-onnx/models/
# ~/.config/ibus-sherpa-onnx/model
# ~/.config/ibus-sherpa-onnx/settings.ini
```

---

## Explicit non-goals

- **🚫** `postinst` network download of the model
- **🚫** Shipping Nemotron weights inside the engine `.deb`
- **🚫** Network download inside the polkit / root helper
- **🚫** System-wide `/usr/share/…/model` active symlink
- **🚫** Parallel user `models/` tree as the normal install target (user path is symlink only)
- **🚫** GSettings schemas for engine prefs (KeyFile instead)
- **🚫** One config file per boolean / hotkey
- **🚫** Linking / depending on OLLMchat / `libochf` / `OLLMrpc` at build or runtime
- **🚫** Relying on shelling out to `tar` for the prefs extract UX (CLI script may still use `tar`)
- **🚫** `MODELS_DIR` / other model-path environment variables
- **🚫** Requiring a hand-crafted `ln -s` after fetch for normal use
- **🚫** Leaving multi-hundred-MB trees in `~/.cache` after a successful install
- **🚫** GNOME Shell extension for a custom tray icon
- **🚫** Full ibus-setup clone (many pages); one prefs window is enough
- **🚫** Depending on RooTerm at build/runtime (copy pattern only)
- **🚫** Changing the user’s session from the agent without ask

## LLM notes

- Implement only after this plan is approved.
- Re-read 0.3 agent rule before any privileged or session command.
- Coding standards: early returns; no noisy one-arg-per-line; no pile of vague helpers (`update_ui` style OK when one clear job).
- Do not mark backlog **✅**; user verifies after package install.
- Prefer `pkexec` over embedding `sudo` in UI strings; polkit helper = move/rm cache only, never wget; user owns the `model` symlink.
